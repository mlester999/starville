# Phase 8D-A owner acceptance

Hosted and two-browser acceptance remains owner-operated. Use distinct eligible wallets and confirm:

1. send, accept, refresh, remove, block, unblock, and re-request friendship behavior;
2. public-safe friend presence with no coordinates, wallet, session, inventory, or block reason;
3. create one party, invite/accept, fill capacity, reject a fifth member, and retain one leader;
4. cross-channel party chat reaches members only; leaving/kicking ends delivery and history access;
5. ready checks are leader-started, self-response-only, revision-safe, and restored after reconnect;
6. leader reconnect within grace preserves leadership and expiry promotes deterministically;
7. promote, kick, disband, suspension, access loss, block, and maintenance reconcile once;
8. refresh/reconnect never duplicates friends, invitations, memberships, or notifications;
9. game UI works by keyboard and at mobile/tablet widths with visible focus and polite updates;
10. administrator pages paginate, reveal no private conversations, and offer no forced membership;
11. unauthorized staff receive 403 and Read-only Analyst has only `social_graph.read`;
12. hosted database lint, pgTAP, integration, and RLS suites pass after explicit approval.

Record request IDs and safe status codes for failures. Do not use production credentials, production
wallets, or a production database for acceptance.
